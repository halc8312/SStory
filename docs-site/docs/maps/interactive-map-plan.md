# インタラクティブマップ計画

Webブラウザ上で動作する**インタラクティブ地図**の実装計画です。

## 概要

エターナル・アルカディアの世界を、ブラウザ上で自由に閲覧・操作できる **Webマップ** を構築します。

### 目的

1. **視覚的な世界理解**: 地図上で世界の姿を把握
2. **ルート検索**: 2地点間の最適経路を探索
3. **データ探索**: Map Data を地図上で可視化
4. **创作支援**: ゲーム・小説制作者向けの参照ツール

---

## v0.1 の位置づけ

現在のポータル（v0.1）は、**計画とプレースホルダーの段階**です。

実際のWebマップ実装は、**別Issue**で進める予定です。

### v0.1 で提供するもの

✅ **本ページ**: 計画の全貌と技術方針の説明
✅ **プレースホルダーJS**: 将来実装用のJavaScript雛形
✅ **将来のリンク**: 実装後のページへの誘導

❌ **実際のWebマップ実装**: 未実装（v0.2以降で対応）

---

## 技術選定

### Webマップライブラリ候補

| ライブラリ | 特徴 | 評価 |
|------------|------|------|
| **Leaflet** | 軽量、シンプル、プラグイン豊富 | ⭐⭐⭐⭐⭐ (推奨) |
| OpenLayers | 高機能、大規模データ向け | ⭐⭐⭐⭐ |
| MapLibre GL JS | ベクトルタイル、3D対応 | ⭐⭐⭐ |
| Google Maps API | 高機能だが有料、カスタム地図には不向き | ⭐⭐ |

**決定**: **Leaflet** を第一候補とする。

**理由**:

- 軽量でGitHub Pages（静的ホスティング）との相性が良い
- プラグインが豊富でカスタマイズしやすい
- 学習コスト低、ドキュメント豊富
- GeoJSON を直接読込可能

---

### 技術スタック想定

```
静的サイト
  ├── MkDocs Material (本ポータル)
  └── HTML + Leaflet (Webマップページ)

データソース
  └── world/map-data/data/*.json (静的JSON)

ホスティング
  └── GitHub Pages (無料、静的ファイルのみ)
```

**重要**: GitHub Pages では **Pythonバックエンドは実行不可**。すべてブラウザ側JavaScriptで処理。

---

## 実装ロードマップ

### Phase 1: 基本ビューア (v0.2)

**目標**: 地図上に nodes と routes を表示する最小実装。

#### 機能

- [ ] Leaflet の導入
- [ ] 世界地図画像をタイルレイヤーとして表示
- [ ] nodes.json を読み込み、マーカー表示
- [ ] routes.json を読み込み、ライン表示
- [ ] クリックでノード詳細表示（ポップアップ）

#### 実装ファイル

```
docs-site/docs/maps/
├── interactive.html   (または interactive.md + JS)
└── assets/js/
    └── map-viewer.js
```

---

### Phase 2: レイヤーとフィルタ (v0.3)

**目標**: 表示するデータを選択可能にする。

#### 機能

- [ ] レイヤー切り替え（nodes, routes, hazards の表示/非表示）
- [ ] 交通種別フィルタ（陸路、海路、空路）
- [ ] 危険区域の色分け表示
- [ ] ノード種別アイコンの違い（都市、村、拠点、天然）

---

### Phase 3: ルート検索 (v1.0)

**目標**: 出発地から目的地までの経路を検索・表示。

#### 機能

- [ ] 出発地・目的地の選択UI
- [ ] 交通種別フィルタ（陸路/海路/空路/組み合わせ）
- [ ] ルート計算（JavaScript版 route_finder.py の移植）
- [ ] 複数経路の提示（最短、最速、安全など）
- [ ] 季節運行判定（季節によって通行不可ルート）
- [ ] ルートの地図上への描画

**技術**:

- Pythonの `route_finder.py` のロジックを JavaScript へ移植
- ブラウザ上での計算（クライアントサイド）

---

### Phase 4: 地図画像オーバーレイ (v1.1)

**目標**: 生成済みの静的地図画像を背景として利用。

#### 機能

- [ ] 世界地図画像（world-map.png）をベースレイヤーとして表示
- [ ] 大陸地図画像への切り替え
- [ ] 地図画像とMap Data（ノード・ルート）の位置合わせ（キャリブレーション）
- [ ] 複数の地図画像レイヤー切り替え

**課題**: 地図画像とGeoJSON座標の座標系合わせが必要。

---

### Phase 5: タイルマップ対応 (v1.2)

**目標**: 大規模なタイル地図をスムーズに表示。

#### 機能

- [ ] タイルレイヤーの導入（高ズームレベル）
- [ ] 特定地域の詳細タイルマップ生成（アストラリス首都圏など）
- [ ] タイルキャッシュによる高速表示

---

### Phase 6: 高度機能 (v2.0)

**目標**: よりリッチな体験。

#### 機能候補

- [ ] ノード詳細情報パネル（人口、政治、経済）
- [ ] タイムライン表示（歴史 event を時系列で）
- [ ] 種族・文化分布のヒートマップ
- [ ] 魔法系統の分布表示
- [ ] 3D表示（MapLibre GL JS による）
- [ ] ユーザー保存ルート

---

## 現在のプレースホルダー

### assets/js/map-viewer-placeholder.js

v0.1では、以下の内容のプレースホルダーJavaScriptファイルを用意します。

```javascript
// インタラクティブマップ実装予定（v0.2以降）
// 現在はプレースホルダー

console.log("Interactive map will be implemented in v0.2+");

// 将来実装予定:
// 1. Leaflet 初期化
// 2. nodes.json 読込
// 3. routes.json 読込
// 4. レイヤー表示制御
// 5. ルート検索UI
```

---

## 技術的課題

### 1. 座標系の整合性

**問題**: 地図画像とGeoJSONデータの座標系が一致するか。

**解決策**:

- 地図画像作成時にGeoJSON座標を考慮して描画
- 必要に応じて座標変換（アフィン変換）
- キャリブレーション用ツール作成（将来）

---

### 2. データ量とパフォーマンス

**問題**: nodes.json が今後数千～数万ノードに増加する可能性。

**解決策**:

- クラスタリング（ズームアウト時はクラスタ表示）
- データのLazy Load（必要部分のみ読込）
- タイルマップ化で効率化

---

### 3. ルート検索の複雑性

**問題**: 単純な最短距離ではなく、交通種別・季節・危険度を考慮したルート計算が必要。

**解決策**:

- 重み付きグラフとして扱う
- A* アルゴリズムや Dijkstra をベースにカスタム関数
- 季節判定ロジックを別関数として実装

---

### 4. GitHub Pages の制約

**問題**: サーバーサイドPythonが使えないため、既存のPythonツールは直接実行不可。

**解決策**:

- Pythonツールはビルド時（GitHub Actions）用に限定
- 本番サイトではJavaScriptで同等機能を実装
- データ生成はローカル/CIで、結果を静的JSONとして配置

---

## データ設計の詳細

### GeoJSON スキーマ

`world/map-data/schemas/` にスキーマ定義があるので、それに従う。

#### node-schema.json 主要ルール

- `id`: 一意な文字列（node_で始まる）
- `type`: Feature 固定
- `geometry.type`: Point 固定
- `properties.name`: 必須
- `properties.type`: city/village/station/ruin/natural いずれか

#### route-schema.json 主要ルール

- `geometry.type`: LineString または MultiLineString
- `properties.start_node`, `properties.end_node`: `nodes.json` に存在するID

---

## テスト計画

### 単体テスト

- [ ] GeoJSON スキーマバリデーション
- [ ] ノードID重複チェック
- [ ] ルートの開始・終了ノード存在チェック

### 結合テスト

- [ ] 地図表示が正しく行われるか
- [ ] ズーム・パンがスムーズか
- [ ] ルート検索結果が妥当か
- [ ] モバイル表示で崩れないか

---

## 今後のスケジュール（予定）

| バージョン | 目標 | 時期 |
|------------|------|------|
| v0.2 | 基本ビューア（nodes/routes表示） | 2026-Q3 |
| v0.3 | レイヤー・フィルタ | 2026-Q3 |
| v1.0 | ルート検索UI | 2026-Q4 |
| v1.1 | 地図画像オーバーレイ | 2027-Q1 |
| v1.2 | タイルマップ試作 | 2027-Q2 |
| v2.0 | 高度機能（3D、保存など） | 2028+ |

---

## 関連Issue・参照ファイル

### 既存のPythonツール

- `tools/map/route_finder.py`: ルート検索ロジック（Python版）
- `tools/map/validate_map_data.py`: データ検証
- `tools/map/export_geojson.py`: GeoJSONエクスポート

これらは**ビルド時・開発時**に使用。GitHub Pages上ではJavaScript版を使用。

---

## よくある質問

### Q: なぜPythonではなくJavaScriptなのか？

**A**: GitHub Pagesは静的ホスティングのため、サーバーサイドコードを実行できません。すべてブラウザ側で処理する必要があるため、JavaScript（またはWebAssembly）が必須。

---

### Q: いつから実際に使えるようになりますか？

**A**: v0.2で最小実装、v0.3で实用レベル、v1.0でルート検索完成を予定。詳細はロードマップ参照。

---

### Q: スマートフォンでも使えますか？

**A**: Leafletはレスポンシブ対応なので、モバイルブラウザでも利用可能。v0.2以降でタッチ操作対応予定。

---

### Q: Map Data の更新は自動で反映されますか？

**A**: GitHub Pagesは静的ファイルなので、 `world/map-data/data/` が更新されたら、`docs-site/docs/data/map/` へのコピーが必要（現在は手動、将来的にCI自動化予定）。

---

## 関連ページ

- [Map Data 紹介](map-data.md)
- [地図ギャラリー](gallery.md)
- [ロードマップ](../index.md) - 全体の開発ロードマップ
- [交通](../world/transportation.md) - ルート検索の基礎情報

---

**最終更新**: 2026-05-03  
**担当**: halc8312
